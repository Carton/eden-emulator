# Eden 模拟器性能优化系列说明

> 英文版见 [OPTIMIZATIONS.md](./OPTIMIZATIONS.md)。
>
> 本文档是本优化系列的完整说明：每项优化的动机、设计、实现与验证数据，
> 以及本系列与历史工作分支的对应关系。目标读者是后续维护者（或未来的自己）——
> 不需要翻过程记录就能理解**系列里每一个补丁为什么存在、如何被验证**。
>
> 基线：`origin/master` @ `74ccea3def`。系列共 14 个提交（3 构建、5 修复、6 优化），
> 全部按主题重新组织，不包含任何诊断插桩与实验开关。
> 过程记录（82 个原始提交、profiling 数据、失败实验）保存在
> `test/master-profiling`（文档型分支）与 `test/p2-draw-resolver`（并行化实验分支），
> 本文档结尾给出索引。

---

## 0. 摘要

以 TOTK（塞尔达传说：王国之泪）水塘场景作为基准场景，通过 ETW（CPU 采样、
线程调度）、AMD uProf IBS（逐指令访存采样）与模拟器内建帧时 CSV 三层观测手段，
对 eden 模拟器做了数周的瓶颈定位与优化。本系列收录其中经过验证、有效或必要的改动：

| 类别 | 数量 | 代表收益 |
|---|---|---|
| CPU（dynarmic JIT） | 2 | 消除 51% 的跨核重复编译；寄存器分配反查从线性扫描降到常数时间 |
| GPU 线程（串行路径） | 3 | 每次绘制调用的冗余地址翻译消除（最高 100% 命中）、冗余寄存器写跳过、管线查找缓存 |
| 呈现时序 | 1 | 解锁模式下呈现时间量化误差小于 1ms，且不再占满一个核心 |
| 正确性修复 | 5 | 加载屏花屏、菜单节奏异常、非确定性进程崩溃等 |
| 构建修复 | 3 | libc++ 可移植性、静态 Qt 交叉构建、MSVC CRT 符号补齐 |

总体效果（水塘基准，解锁模式，90 秒测量窗口，每个配置至少 2 局）：
- 帧率中位数从 43.5±0.5 的历史区间提升到稳定的 44.1-44.4 fps；
- 帧时中位数稳定落在 22.50ms 档位，不再出现 21.67/23.33ms 两档跳动；
- p99 帧时从 32.5ms 降到 31.7ms；
- 旋转镜头时的帧率尖刺减少 70%-79%（管线查找缓存）；
- 加载屏的视觉垃圾（采样到从未写入的显存）消除。

明确不在本系列的内容（验证为负收益，或属于诊断设施，见 §6）：按绘制调用粒度的
并行化全线尝试（draw token / DrawResolver / tail-on-worker，均为净负收益）、
全部 profiling 插桩（计数器、时钟、块转储）、实验性 A/B 环境变量开关。

---

## 1. 方法论

观测手段分三层：
1. **ETW（wpr 采集 + 性能分析器）**：CPU 采样栈、线程调度、内核时间——用来判断
   瓶颈在 CPU 还是 GPU，并定位函数级热点（eden 的线程名可直接读出：
   `GPU`、`CPUCore_*`、`VulkanWorker`、`HostTiming`）。
2. **AMD uProf IBS**：旋转场景 160 秒逐指令采样——区分"执行吞吐瓶颈"与
   "访存/分支停顿"。结论：GPU 线程的访存未命中延迟只占周期的 2.33%、
   分支误预测占 0.40%，即纯粹的执行吞吐瓶颈。这决定了优化方向是
   "删掉冗余指令"，而不是调整数据布局或预取。
3. **内建帧时 CSV**：`record_frame_times` 每局导出帧时序列，
   统计 fps / 中位数 / p95 / p99 与画面平均亮度。

基准规则（场景负载随游戏内时间变化，见 §7）：
- 同一配置至少跑 2 局才下结论；
- 用画面平均亮度作协变量区分昼夜与天气相位（日间带约 56.8，黄昏带 36-48），
  不同亮度带之间不直接比较绝对帧率；
- 帧时中位数会落在 1.667ms 整数倍的量化档位上，要区分"档位切换"与
  "真实负载变化"；
- 每局检查三件事：确认进入游戏、截图签名正常、模拟器正常关闭。

判定标准：宏观指标（fps / 中位数 / p99）与微观局部指标（如快路径命中率、
被消除的翻译次数）双重证据；单项改动的影响在 ±2% 以内时，
以局部指标加"无回归"作为保留依据。

---

## 2. CPU 侧：dynarmic JIT（2 个提交）

### 2.1 进程级跨核共享 IR 缓存 — `d46d08038b`

原始提交：a421ed22b4（实验版）→ 88fc516432（默认开启 + 512MiB 上限）→
bb59b29f1d（适配 master 的扁平 terminal）→ 0df87fd502（复用校验与上限加固）→
74af90ee2c（恢复 512MiB 默认）+ 测试 790b899833/e5fcc1b183。

问题：四个 CPU 核各自持有独立的 Jit 实例。块转储（block dump）分析显示，
51% 的块编译是跨核重复劳动——同一段 guest 代码块被 2-3 个核各自完整地
翻译和优化一遍。编译发生在前台，是进入新区域时卡顿的主要来源之一，
重复编译是纯粹的浪费。

设计：
- 缓存**优化完成后**（post-Optimize）的 IR 块：命中时跳过翻译和优化两个阶段，
  只执行各核自己的代码生成（寄存器分配是每核状态，无法共享）；缓存为进程级单例，
  以 IR LocationDescriptor 为键。
- 复用安全性三重校验：条目同时锁定逐字的 guest 机器码（FNV 哈希加逐词比对）、
  翻译配置元组（unpredictable 行为、wall clock、halt-on-access、cache hook、
  优化开关、dczid、polyfill 选项中任一项不同即失效）、块起止 PC。
  机器码或配置不匹配时回退到完整编译。
- 生命周期：最后一个 Jit 实例析构时清空缓存，避免配置漂移跨会话污染。
- 内存上限 512MiB，按 LRU 驱逐；TOTK 整局稳态实测 335MiB，不触顶。
- 开关：Windows 下默认开启；`EDEN_JIT_IRCACHE=0` 全局关闭，
  `EDEN_JIT_IRCACHE_MAXBYTES` 调整预算。`IRCacheStats` 只在编译路径上做
  原子计数（命中/存储/条目数/字节数），供单元测试断言使用，
  不进入稳态执行路径。

验证：块转储去重分析；多核执行等价性测试（真实 CPU 核）；TOTK 整局运行，
帧率在历史区间内。

### 2.2 寄存器分配的常数时间值定位 — `1e6329e5d2`

原始提交：82a043bc40 + 测试 028c82613c。

问题：`ValueLocation(inst)` 通过线性扫描每个 host 寄存器位置的值列表来定位
某个 IR 值；大代码块的代码生成阶段，这个扫描主导了寄存器分配的耗时。

设计：IR 指令名在单个块内是稠密的（NamingPass 保证为 1..N），因此用一个
4096 项的 u8 表 `name → hostloc+1` 直接反查；表随块归零（RegAlloc 每次代码
生成时 placement-new）。寄存器移动/交换时重新登记受影响的位置；未命名值和
越界名回退到原线性扫描；debug 构建对每次反查做交叉核对。

---

## 3. GPU 线程串行路径（3 个提交）

GPU 线程（命令解析 → 状态机 → 绑定解析 → 命令录制）是模拟器的主瓶颈线程。
IBS 采样证明它是执行吞吐瓶颈（不存在内存墙或分支墙），
因此这里的优化全部是"删除冗余工作"。

### 3.1 冗余状态与绑定工作消除 — `22a7e999e2`

原始提交：9aa4ce0921（GPU 命令线程微优化；ETW 验证 GPU 线程自耗时在
100 秒窗口内从 105.9s 降到 102.4s）+ 5da3c43261（跨通道 buffer_id 失效修复）
+ 测试。

四项改动：
1. **相同值的寄存器写跳过**：游戏每次绘制都会全量重写寄存器状态；
   `ProcessDirtyRegisters` 对 `regs[method] == argument` 的写直接返回，
   跳过存储与脏标记（自耗时 4.23s → 1.66s）。同时引入 `change_generation`
   计数器（只在值真正变化时递增），供下游缓存使用（见 3.2）。
2. **LRU 触碰去重**：Buffer 与 Image 记录 `last_touch_tick`，同一帧内重复的
   LRU Touch 直接跳过（TouchBuffer 1.48s → 0.55s，纹理 Touch 1.27s → 0.30s）。
3. **SSBO/TBO 的 buffer_id 保持**：绑定的目标（地址加大小）不变时保留已解析的
   buffer_id，`Update*Buffers` 据此跳过每次绘制的 FindBuffer；
   `DeleteBuffer` 改为对所有活跃通道（channel）失效已解析的 id，
   保证槽位复用的安全。
4. **uniform 对齐查询缓存**：构造时缓存 `GetUniformBufferAlignment()` 的结果，
   免去每次绑定时的虚函数调用。

说明：单独看这个提交，帧率没有变化——当时四个热点线程全部饱和，
帧时间由最慢的一级决定。它的价值在于降低 GPU 线程压力，为后续优化腾出空间。

### 3.2 图形管线查找缓存 — `fcac5d10fd`

原始提交：d900d7c2ad（generation 缓存）+ be3e692dac/9af2007176
（transition 键哈希）。

1. **按寄存器代数缓存**：`CurrentGraphicsPipeline` 比较 Maxwell3D 的
   change_generation 与上次构建管线时的代数，没变就直接返回 current_pipeline，
   跳过阶段刷新、固定状态刷新和 transition/map 的键比较。寄存器没有真正变化的
   绘制是常态：游戏每次绘制都重写相同的寄存器。
2. **transition 键哈希预计算**：`AddTransition` 时预先存好键的哈希，查找先比
   哈希再逐字节比较；只有一个候选时直接做等值比较——这种场景先算哈希只会更慢。

验证：旋转镜头场景的帧率尖刺减少 79%/70%，中位数持平。

### 3.3 地址翻译冗余消除 — `0781b56252`

原始提交：18aaa38cef（描述符直读初版）+ 74d5550276（串行优化第一批，
帧率 +0.5-1%）+ e7c1484690/e43f875f75/814f80c107（描述符/SSBO/脏位图三项）+
8398d87199（staging 移位）。后五个经中间验证分支（test/master-profiling
c31cc64c1f）合入本系列。

六项改动，括号内为水塘单局的消除率或收益：
1. **描述符表读取的单页判定**：描述符不超过 32 字节，用算术判断确认整个描述符
   落在同一个设备页内后，第二次地址翻译可以完全省掉（84.3M/84.3M = 100%，
   单局省约 8400 万次翻译）；跨页情形保留原来的连续性检查。
2. **SSBO 双四字节一次翻译**：ssbo_addr 与 +8 两个四字（qword）同页时，
   一次翻译读出两个（7.8M/7.8M = 100%）。
3. **SSBO 成对翻译的等价推导**：非对齐地址与对齐地址同页时，设备地址直接用
   "对齐地址 + 偏移"推出（7.8M/7.8M = 100%）；跨页时走原路径，
   绝不跨页推断连续性。
4. **GPU 脏位图的单字查询**：只落在单个位图字内的查询直接索引顶层表
   （且不为查询创建缺失的 manager），manager 级直接构造页掩码，
   不再经过 IterateWords（92.8% 的查询命中）；只改查询路径，
   异常区间保持原语义。
5. **uniform 单页拷贝快路径**：guest 到 host 的流式拷贝绕过 ReadBlockUnsafe
   的逐页分发，两端指针连续时直接 memcpy（图形与计算两条路径）；
   FlushCaching 改为直接传递 span，不再每次触发累加器都把整个失效列表
   拷贝一遍。
6. **StagingBufferPool::Region 的移位替代除法**：常规路径的 region 大小是
   16MiB（2 的幂），运行时 64 位除法换成预计算的移位（shift=24）。每次
   GetStreamBuffer 有 4 处除法，编译器无法合并消除；非 2 的幂的 region
   大小保留除法。

验证：第一批改动实测帧率 +0.5-1%（44.31/43.64，对照 43.5±0.5 的历史区间）；
组合验收 43.8-44.4 fps、帧时中位数 22.50ms；各项翻译消除率如上。

---

## 4. 呈现时序（1 个提交）

### 4.1 解锁模式下的 vsync 调度频率下限 — `28415c31f9`

原始提交：edcd160726（600Hz 下限，自 v0.2.1 的 c8b0c853f8 适配）→
18aaa38cef 中的量化粒度减半（600→1200Hz）。

问题链：解锁帧率时，conductor 以无下限的周期调度 guest vsync 事件，
实际会出现数千赫兹的唤醒风暴，把 HostTiming/VSync 线程占满一整个核心。
把下限提到 600Hz 修好了这个问题，但呈现时间被量化到 1.667ms 的栅格上——
一帧的工作量刚好落在栅格边界上方时，帧时中位数会在 21.67 与 23.33ms
（13 与 14 个 tick）之间来回跳。

设计：`kMinVsyncTickNs = 1e9/1200`——量化粒度减半到 0.833ms
（量化损失小于 1ms），唤醒频率仍有硬上限。

验证：帧时中位数稳定落在 22.50ms 档；HostTiming 占满整核的现象消失。

---

## 5. 正确性修复（5 个提交）

| 提交 | 原始 | 内容 |
|---|---|---|
| `ccd22ee643` | 9e8edc411a | TextureBufferBinding::format 显式给 Invalid 默认值。默认初始化的通道绑定在首次空绑定比较时会读取未初始化的枚举值；附带投毒存储的回归测试 |
| `dc6f9e9f66` | f3b7712ae3 | 异步 ASTC 解码窗口的零填充。从 QueueAsyncDecode 到下一帧 TickAsyncDecode 上传之间，VkImage 处于未初始化状态，而绘制完全可能在这期间采样它，读到从未写入的显存——加载屏上的垃圾花块，单局观测到 2637 次排队解码。改为排队时立即通过 staging 上传全零，解码窗口内采样到的是黑色。ZeroUploadCopies 镜像 ConvertImage 的缓冲几何（含 BC1/BC3 重压缩分支） |
| `e3a7dc82ba` | 568605ebf7 | 解锁模式下，显式帧间隔请求按硬件速率合成。暂停菜单这类以 30fps 设计的界面（交换间隔 1-4）此前被 0.01 的解锁倍率拖到全速，输入重复速率随之超速。只有游戏自己按动态帧率提交的情况（compose 倍率 >1）才走解锁倍率 |
| `5d50effdf1` | a10393a9b0 | 退出游戏时恢复用户进游戏前的限速偏好。上游在 OnShutdownBegin 强制 `use_speed_limit=true`，等于每次退出游戏都悄悄把"限制速度"勾选回去 |
| `c0eb6ef236` | eb2901fbc0 | settings 文件系统异常兜底。Windows 上 `rename(tmp, dat)` 在目标被并发占用时抛出 filesystem_error，而 StoreSettingsFile 运行在 TimeWorker 线程上无人捕获，直接 terminate，不定时以 0xC0000409 杀死进程。经 SIGABRT 调用栈定位：TimeWorker→SetNetworkSystemClockContext→SetSaveNeeded→StoreSettings；现场证据是存档目录里同时残留 tmp 与 dat 文件。改为函数级 try 块捕获、记日志并返回 false，成功路径逐字节不变 |

---

## 6. 构建修复（3 个提交）与负结果归档

构建：`bcdf43e3ad`（libc++ 与更严格的 STL 配置下缺失的标准头显式包含）、
`87498a196d`（静态 Qt 与交叉构建跳过 windeployqt）、
`056bcbddd2`（CPM 静态 Qt 6.11.1 需要的 MSVC CRT `__std_*` 向量符号补齐 shim；
重建 build 目录时须挂到 `CMAKE_EXE_LINKER_FLAGS`，用法见文件头）。

验证为负收益、未收录的路线（完整过程见 test/master-profiling 的
PROFILE_PROGRESS.md）：
- **按绘制调用并行化的全线尝试**（test/p2-draw-resolver）：深度为 1 的
  DrawResolver（单解析线程）净负 10fps——快照加跨线程交接的成本，
  大于每次绘制约 1µs 的可并行工作量；draw token 的 1A/1B/2A/2B 四个阶段
  全部提前终止，最好的版本仍比直接内联执行慢 14.3%；tail-on-worker FIFO
  同样验证为负。结论：在本架构下，绘制级绑定解析并行化被交接成本的算术
  堵死；调研 RPCS3、PCSX2、Dolphin 等同类项目也没有找到先例。
- **诊断设施**：JIT 计数器、指令构成统计、块转储（EDEN_JIT_BLOCKDUMP）、
  uniform 流拷贝影子统计（EDEN_UNIFORM_STATS），以及各类环境变量门控的
  插桩——测量完成后全部剥离。其中 Windows 默认开启的 block_lookups
  原子计数位于热分发路径上，剥离后 p99 从 32.5 降到 31.7ms。
- **实验开关**：EDEN_JIT_NOLINK/NORSB/NOFASTDISPATCH 分发路径 A/B 门。
- **零收益**：serial 34.3 的 FlushWork/FlushCaching 跳过门（实测零变化）；
  epoch/memcmp 内容比对缓存（与 token 路线绑定）。

---

## 7. 基线与验收数据汇总

基准协议：`bench_run.py`（启动 → 自动进入游戏 → 90 秒测量 → 优雅关闭 →
帧时 CSV 解析），水塘测试存档，解锁模式，RelWithDebInfo 构建，同一台机器。

| 日期/构建 | fps | 中位数(ms) | p99(ms) | 亮度 | 备注 |
|---|---|---|---|---|---|
| 优化前 master 基线 | 43.5±0.5 | 22.50 | 31.7-32.5 | ~56.8 | §23/§25.6 基线 |
| 优化移植验收（n=3） | 44.31/44.14/43.69 | 22.50 | 32.5 | 56.5-56.8 | test/master-profiling |
| 剥离诊断后（n=2） | 44.35/44.29 | 22.50 | **31.67** | 36.2/47.6† | |
| 重组提交后 | 43.84 | 22.50 | 33.34 | 47.6† | 与上一行仅差注释 |

† 游戏内世界时钟漂移到黄昏相位（亮度带不同）；帧时中位数档位一致、
帧时分布形态一致，不同亮度带之间不比较绝对帧率。

环境：Windows 11 x64 / Ryzen（12 逻辑核）/ RTX 2060 / VS2022 19.44 /
Qt 6.11.1 静态 / RelWithDebInfo。

---

## 8. 提交索引

| 提交 | 类型 | 主题 | 主要原始提交 |
|---|---|---|---|
| bcdf43e3ad | build | 标准头显式包含 | 96e4a3fc45 f34a54de9c ab96d66675 |
| 87498a196d | build | 静态 Qt 跳过 windeployqt | 8be57d503d |
| 056bcbddd2 | build | MSVC CRT 向量符号 shim | v0.2.1 移植轮（PROFILE §21） |
| ccd22ee643 | fix | TBO 格式默认值 | 9e8edc411a |
| dc6f9e9f66 | fix | 异步 ASTC 零填充 | f3b7712ae3 |
| e3a7dc82ba | fix | 显式帧间隔按硬件速率 | 568605ebf7 |
| 5d50effdf1 | fix | 限速偏好恢复 | a10393a9b0 |
| c0eb6ef236 | fix | settings 异常兜底 | eb2901fbc0 |
| 28415c31f9 | perf | vsync 调度 1200Hz 下限 | edcd160726 + 18aaa38cef |
| d46d08038b | perf | 跨核共享 IR 缓存 | a421ed22b4 88fc516432 bb59b29f1d 0df87fd502 74af90ee2c |
| 1e6329e5d2 | perf | 寄存器分配常数时间反查 | 82a043bc40 |
| 22a7e999e2 | perf | 状态与绑定冗余消除 | 9aa4ce0921 5da3c43261 |
| fcac5d10fd | perf | 管线查找缓存 | d900d7c2ad be3e692dac 9af2007176 |
| 0781b56252 | perf | 地址翻译冗余消除 | 18aaa38cef 74d5550276 e7c1484690 e43f875f75 814f80c107 8398d87199 |
